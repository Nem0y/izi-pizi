#include <iostream>

using namespace std;

int main()
{
  setlocale(0, "");
  int n;
  cout << "Ввести число: ";
  cin >> n;
  if ((n == 1 || n % 2 == 0) && n / 2 !=0 )
    cout << "Не простое число " << endl;
  else
    cout << "Простое число\n" << endl;

  //system("pause") ;
  return 0;
}
